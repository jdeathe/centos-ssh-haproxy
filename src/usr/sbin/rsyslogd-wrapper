#!/usr/bin/env bash

readonly NICE="/bin/nice"
readonly NICENESS="10"

OPTIONS=""

if [[ -f /usr/sbin/rsyslogd ]]
then
	readonly BIN="/usr/sbin/rsyslogd"
else
	readonly BIN="/sbin/rsyslogd"
	OPTIONS+=" -c5"
fi

OPTIONS+="
 -n
 -4
 -Q
 -l 127.0.0.1
 -f /etc/rsyslog.conf
"

exec ${NICE} \
	-n ${NICENESS} \
	${BIN} \
	${OPTIONS}
